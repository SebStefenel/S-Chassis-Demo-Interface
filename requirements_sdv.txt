# ── Unified SDV requirements ─────────────────────────────────────────────────
# Covers both Repo A (deepface) and Repo B (Drowsiness_Detection).
#
# Dependency analysis / conflict notes:
#   - Both repos use opencv-python — same package, no conflict.
#   - dlib is used by deepface (optional detector backend) and directly by
#     Drowsiness_Detection. Same package, no conflict.  Requires cmake and a
#     C++ compiler to build.  On macOS: `xcode-select --install` then pip install.
#     On Apple Silicon you may need: pip install dlib --no-cache-dir
#   - TensorFlow is heavyweight but only used by deepface.  On Apple Silicon
#     use `tensorflow-macos` + `tensorflow-metal` instead of plain `tensorflow`.
#   - No PyTorch / TF conflict because Drowsiness_Detection is pure scipy/dlib.

# ── deepface (Repo A) ─────────────────────────────────────────────────────────
requests>=2.27.1
numpy>=1.24.0,<2.0.0
pandas>=1.3.0
gdown>=3.10.1
tqdm>=4.30.0
Pillow>=9.0.0
opencv-python>=4.5.5.64
tensorflow>=2.12.0
keras>=2.12.0
tf_keras>=2.12.0
Flask>=2.0.0
flask_cors>=4.0.1
mtcnn>=0.1.0
retina-face>=0.0.14
fire>=0.4.0
gunicorn>=20.1.0
lightphe>=0.0.15
lightdsa>=0.0.3
python-dotenv>=1.0.1

# ── Drowsiness_Detection (Repo B) ─────────────────────────────────────────────
scipy>=1.7.0
imutils>=0.5.4
dlib>=19.22.0

# ── Apple Silicon note ────────────────────────────────────────────────────────
# If on macOS arm64, replace the tensorflow lines above with:
#   tensorflow-macos>=2.12.0
#   tensorflow-metal>=0.8.0
